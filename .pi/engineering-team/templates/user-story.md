# Story <n>: <title>

**Status:** Draft | Approved | In Progress | Done
**Created:** <DATE>
**Type:** Feature | Bug | Refactor | Doc

## Background
Why we're doing this. What problem it solves. Who is affected.

## User-facing description
Plain language. As a <user>, I want <capability>, so that <outcome>.

## Acceptance criteria
Testable from the outside. Each criterion gets at least one test.

- [ ] Given <state>, when <action>, then <outcome>.
- [ ] ...

## Out of scope
What this story explicitly does NOT cover. Paste anything tempting that you decided to defer.

## Openness check
Catallax is open, permissionless, and WoT-based. Confirm this story does not assume a privileged actor, gatekeeper, or special pubkey/relay/arbiter. Any default it relies on must be a plain, overridable bootstrapping convenience — anyone should be able to clone and reconfigure for their own purpose. Note here if the request brushes against this (and how it was resolved).

## Open questions
Anything the PO doesn't yet know. Resolve before approving the story.

## Linked artifacts
- ADR: (filled in after Architecture phase)
- Test plan: (filled in after Test Design phase)
- Review: (filled in after Review phase)
